'use strict';
const clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,Number(x)||0));
const profiles={TREND_UP:{hardStop:.055,beTrigger:.07,beLock:.002,trailTrigger:.12,trailGap:.045,staleBars:288},RANGE:{hardStop:.04,beTrigger:.035,beLock:.003,trailTrigger:.055,trailGap:.02,staleBars:144},VOLATILE:{hardStop:.055,beTrigger:.06,beLock:.002,trailTrigger:.10,trailGap:.04,staleBars:180},RISK_OFF:{hardStop:.035,beTrigger:.03,beLock:.003,trailTrigger:.05,trailGap:.018,staleBars:96}};
function blend(weights,opportunity,quality=.5){const out={hardStop:0,beTrigger:0,beLock:0,trailTrigger:0,trailGap:0,staleBars:0};let z=0;for(const [r,w0] of Object.entries(weights||{})){const p=profiles[r];if(!p)continue;const w=Math.max(.02,w0);z+=w;for(const k of Object.keys(out))out[k]+=w*p[k]}for(const k of Object.keys(out))out[k]/=z||1;const trend=weights?.TREND_UP||0,risk=weights?.RISK_OFF||0,conv=clamp(.55*quality+.45*opportunity);out.trailTrigger*=1+.22*trend*conv;out.trailGap*=1+.18*trend*conv;out.staleBars*=1+.35*trend*conv;out.hardStop*=1-.18*risk;out.beTrigger*=1-.22*risk;out.trailGap*=1-.15*risk;out.staleBars*=1-.35*risk;out.staleBars=Math.max(72,Math.min(360,Math.round(out.staleBars)));return out}
module.exports={profiles,blend};
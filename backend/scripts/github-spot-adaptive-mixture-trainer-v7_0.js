'use strict';
const fs=require('fs'),path=require('path');
const BASE='https://data-api.binance.vision',INTERVAL='5m',STEP=300000;
const DAYS=Math.max(7,Math.min(30,Number(process.env.TRAIN_LOOKBACK_DAYS||30)));
const MAX_SYMBOLS=Math.max(20,Math.min(80,Number(process.env.TRAIN_MAX_SYMBOLS||60)));
const MIN_QV=Math.max(100000,Number(process.env.MIN_HIST_QUOTE_VOL_24H||200000));
const COST=.004,WARM=288,FWD=288,PURGE=6*3600000,COOLDOWN=6*3600000,HARD_STOP=.05,BE_TRIGGER=.05,BE_LOCK=.002,TRAIL_TRIGGER=.08,TRAIL_GAP=.03,STALE_BARS=216;
const avg=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0;
const ret=(a,b)=>a>0?b/a-1:0;
const clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,Number(x)||0));
const pct=(xs,p)=>{if(!xs.length)return 0;const a=[...xs].sort((x,y)=>x-y);return a[Math.max(0,Math.min(a.length-1,Math.floor((a.length-1)*p)))]};
const qsum=(r,i,n)=>{let s=0;for(let k=Math.max(0,i-n+1);k<=i;k++)s+=r[k].q;return s};
async function j(url){const c=new AbortController(),t=setTimeout(()=>c.abort(),25000);try{const r=await fetch(url,{signal:c.signal,headers:{'user-agent':'proypers25-v7.0-adaptive-mixture'}});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}finally{clearTimeout(t)}}
async function universe(){const rows=await j(`${BASE}/api/v3/ticker/24hr`);return rows.filter(x=>String(x.symbol).endsWith('USDT')).filter(x=>!/(UP|DOWN|BULL|BEAR)USDT$/.test(String(x.symbol))).filter(x=>Number(x.quoteVolume)>=MIN_QV).sort((a,b)=>Number(b.quoteVolume)-Number(a.quoteVolume)).slice(0,MAX_SYMBOLS).map(x=>String(x.symbol))}
async function klines(symbol){let cur=Date.now()-DAYS*86400000,out=[];for(let p=0;p<40&&cur<Date.now();p++){const u=new URLSearchParams({symbol,interval:INTERVAL,startTime:String(cur),limit:'1000'}),rows=await j(`${BASE}/api/v3/klines?${u}`);if(!Array.isArray(rows)||!rows.length)break;for(const r of rows)out.push({t:+r[0],o:+r[1],h:+r[2],l:+r[3],c:+r[4],q:+r[7],n:+r[8]});const nx=+rows.at(-1)[0]+STEP;if(nx<=cur)break;cur=nx;if(rows.length<1000)break}return out}
function btcMap(r){const m=new Map();for(let i=WARM;i<r.length;i++){const rr=r.slice(i-12,i+1).map((b,k,a)=>k?ret(a[k-1].c,b.c):0).slice(1);m.set(r[i].t,{r60:ret(r[i-12].c,r[i].c),r240:ret(r[i-48].c,r[i].c),r24:ret(r[i-288].c,r[i].c),vol60:Math.sqrt(avg(rr.map(x=>x*x)))})}return m}
function candleMicro(r,i){const w=r.slice(i-11,i+1);let hh=0,hl=0,bull=0,upper=0,body=0,vr=[];for(let k=0;k<w.length;k++){const b=w[k],rg=Math.max(1e-12,b.h-b.l);body+=Math.abs(b.c-b.o)/rg;upper+=(b.h-Math.max(b.o,b.c))/rg;if(b.c>b.o)bull++;if(k>0){if(b.h>w[k-1].h)hh++;if(b.l>w[k-1].l)hl++}vr.push(b.q)}const first=w[0],last=w.at(-1),peak=Math.max(...w.map(x=>x.h)),pullback=(peak-last.c)/Math.max(1e-12,last.c),v0=avg(vr.slice(0,8)),v1=avg(vr.slice(8));return {hhRate:hh/11,hlRate:hl/11,bullRate:bull/12,upperWick:upper/12,bodyShare:body/12,pullback,volumePersistence:v0>0?v1/v0:1,trendEfficiency:Math.abs(last.c-first.o)/Math.max(1e-12,w.reduce((s,b)=>s+Math.abs(b.c-b.o),0))}}
function feat(r,i,bm){const c=r[i].c,b=bm.get(r[i].t)||{},r15=ret(r[i-3].c,c),r30=ret(r[i-6].c,c),r60=ret(r[i-12].c,c),r240=ret(r[i-48].c,c),r24=ret(r[i-288].c,c),base=avg(r.slice(i-72,i-12).map(x=>x.q))*12,ph60=Math.max(...r.slice(i-12,i).map(x=>x.h)),vol15=base>0?qsum(r,i,3)/(base/4):1,tradeAccel=r[i].n/Math.max(1,avg(r.slice(i-12,i).map(x=>x.n))),breakout60=ph60?c/ph60-1:0,rs60=r60-(b.r60||0),rs240=r240-(b.r240||0);return {r15,r30,r60,r240,r24,qv:qsum(r,i,288),vol15,tradeAccel,breakout60,rs60,rs240,btcR60:b.r60||0,btcR240:b.r240||0,btcR24:b.r24||0,btcVol60:b.vol60||0,...candleMicro(r,i)}}
const agents={EARLY:f=>1.3*f.r15+1.1*f.r30+.6*f.r60+.35*Math.log(Math.max(.2,f.vol15))-.75*Math.max(0,f.r24-.12),VOLUME:f=>.75*Math.log(Math.max(.2,f.vol15))+.55*Math.log(Math.max(.2,f.tradeAccel))+.55*f.r15+.3*f.breakout60,BREAKOUT:f=>1.6*f.breakout60+.9*f.r15+.45*f.r30+.35*Math.log(Math.max(.2,f.vol15))-.8*Math.max(0,f.r24-.10),RS:f=>1.35*f.rs60+.9*f.rs240+.35*f.r30+.2*Math.log(Math.max(.2,f.vol15)),PRE:f=>1.05*f.r15+.8*f.r30+.6*f.breakout60+.45*Math.log(Math.max(.2,f.vol15))+.45*f.rs60-.75*Math.max(0,f.r24-.10)};
function outcome(r,i){const e=i+1;if(e>=r.length)return null;const entry=r[e].o;if(!(entry>0))return null;let high=entry,low=entry,stop=entry*(1-HARD_STOP),exit=entry,exitI=e,hit10=null,stopBefore10=false;for(let k=e;k<r.length&&k<=i+FWD;k++){const b=r[k];high=Math.max(high,b.h);low=Math.min(low,b.l);if(hit10===null&&b.h/entry-1>=.10)hit10=(k-e)*5;if(hit10===null&&b.l<=entry*(1-HARD_STOP))stopBefore10=true;if(b.l<=stop){exit=stop;exitI=k;break}const hg=high/entry-1;if(hg>=TRAIL_TRIGGER)stop=Math.max(stop,high*(1-TRAIL_GAP));else if(hg>=BE_TRIGGER)stop=Math.max(stop,entry*(1+BE_LOCK));if(k-e>=STALE_BARS&&hg<=.005){exit=b.c;exitI=k;break}exit=b.c;exitI=k}const mfe=high/entry-1,mae=low/entry-1,pathRatio=mfe/Math.max(.005,Math.abs(mae)),net=exit/entry-1-COST,clean=hit10!==null&&hit10<=720&&!stopBefore10&&mae>=-.04&&pathRatio>=2;return {net,clean,mfe,mae,pathRatio,hit10,stopBefore10,holdingMin:(exitI-e)*5}}
function thresholds(train,q){const out={};for(const [n,fn] of Object.entries(agents))out[n]=pct(train.map(s=>fn(s.f)),q);return out}
function state(s,th){let passed=0,margin=0;for(const [n,fn] of Object.entries(agents)){const v=fn(s.f),t=th[n];if(v>=t)passed++;margin+=clamp((v-t)/Math.max(.10,Math.abs(t)),-1,2)}return {passed,margin:margin/Object.keys(agents).length}}
function microCal(train){const keys=['hhRate','hlRate','bullRate','bodyShare','volumePersistence','trendEfficiency','pullback','upperWick'],c={};for(const k of keys){const xs=train.map(s=>s.f[k]).filter(Number.isFinite);c[k]={lo:pct(xs,.2),hi:pct(xs,.8),neg:k==='pullback'||k==='upperWick'}}return c}
function microScore(f,c){let s=0,w=0;for(const [k,z] of Object.entries(c)){const d=Math.max(1e-12,z.hi-z.lo);let v=clamp((f[k]-z.lo)/d);if(z.neg)v=1-v;const wt=(k==='hlRate'||k==='volumePersistence'||k==='trendEfficiency'||k==='pullback')?1.3:1;s+=wt*v;w+=wt}return w?s/w:.5}
function dedupe(sig){const out=[],until=new Map();for(const s of [...sig].sort((a,b)=>a.t-b.t)){if(s.t<(until.get(s.symbol)||0))continue;out.push(s);until.set(s.symbol,s.t+COOLDOWN)}return out}
function breadthFrom(map,t){const b=map.get(t);if(!b||!b.n)return {up15:.5,up60:.5,breakout:.5,ignite:.5,mean60:0};return {up15:b.up15/b.n,up60:b.up60/b.n,breakout:b.breakout/b.n,ignite:b.ignite/b.n,mean60:b.sum60/b.n}}
function regimeWeights(f,b){const trend=clamp(.25+5*Math.max(0,f.btcR240)+2*Math.max(0,f.btcR60)+.7*Math.max(0,b.up60-.5)+.5*Math.max(0,b.breakout-.35));const risk=clamp(.15+5*Math.max(0,-f.btcR240)+2*Math.max(0,-f.btcR60)+18*Math.max(0,f.btcVol60-.01)+.8*Math.max(0,.45-b.up60));const vol=clamp(.15+22*Math.max(0,f.btcVol60-.007)+.6*Math.max(0,b.ignite-.25));const range=clamp(.35+.7*(1-Math.abs(clamp(.5+10*f.btcR240)-.5)*2)+.4*(1-Math.abs(b.up60-.5)*2));const raw={TREND_UP:trend,RANGE:range,VOLATILE:vol,RISK_OFF:risk},z=Object.values(raw).reduce((a,x)=>a+x,0)||1;for(const k of Object.keys(raw))raw[k]/=z;return raw}
function opportunityScore(b){return clamp(.30*b.up15+.30*b.up60+.20*b.breakout+.20*b.ignite)}
function localMetrics(samples,th,cal,cfg){const allClean=new Set(samples.filter(s=>s.o.clean).map(s=>`${s.symbol}:${Math.floor(s.t/COOLDOWN)}`)),sig=dedupe(samples.filter(s=>state(s,th).passed>=cfg.consensus));let eq=1,peak=1,dd=0,sizeSum=0;const admitted=[],det=new Set();for(const s of sig){const st=state(s,th),ms=microScore(s.f,cal),baseQ=clamp((st.passed-cfg.consensus+1)/Math.max(1,6-cfg.consensus)),marginQ=clamp((st.margin+.15)/.75),quality=clamp((1-cfg.microWeight)*(.65*baseQ+.35*marginQ)+cfg.microWeight*ms);if(quality<cfg.minQuality)continue;const size=cfg.minSize+(cfg.maxSize-cfg.minSize)*Math.pow(quality,cfg.gamma);admitted.push({...s,size,quality});sizeSum+=size;eq*=1+size*s.o.net;peak=Math.max(peak,eq);dd=Math.min(dd,eq/peak-1);if(s.o.clean)det.add(`${s.symbol}:${Math.floor(s.t/COOLDOWN)}`)}const clean=admitted.filter(s=>s.o.clean);return {signals:sig.length,admitted:admitted.length,clean:clean.length,winRate:admitted.length?admitted.filter(s=>s.o.net>0).length/admitted.length:0,precision:admitted.length?clean.length/admitted.length:0,recall:allClean.size?det.size/allClean.size:0,avgNetRet:avg(admitted.map(s=>s.o.net)),netGrowth:eq-1,maxDrawdown:dd,avgDetection24hPct:avg(clean.map(s=>s.f.r24*100)),stopBefore10Rate:admitted.length?admitted.filter(s=>s.o.stopBefore10).length/admitted.length:0,avgSize:admitted.length?sizeSum/admitted.length:0}}
function specialistConfigs(){const out=[];for(const q of [.80,.84,.88,.92])for(const consensus of [2,3,4])for(const microWeight of [.2,.4,.6])for(const minQuality of [.30,.40,.50])for(const maxSize of [.06,.09,.12])out.push({q,consensus,microWeight,minQuality,minSize:.015,maxSize,gamma:1.15});return out}
function specialistObjective(m){if(m.admitted<5)return-1e9;const ddPenalty=Math.max(0,Math.abs(m.maxDrawdown)-.10)*14,partPenalty=Math.max(0,.08-m.recall)*3;return m.netGrowth*6+m.avgNetRet*18+m.winRate*.65+m.precision*.85+m.recall*1.2-m.stopBefore10Rate*.5-ddPenalty-partPenalty}
function chooseSpecialists(train,val,cal){const out={};for(const rg of ['TREND_UP','RANGE','VOLATILE','RISK_OFF']){const tr=train.filter(s=>s.primaryRegime===rg),va=val.filter(s=>s.primaryRegime===rg);let best=null;if(tr.length>=100&&va.length>=20){for(const cfg of specialistConfigs()){const th=thresholds(tr,cfg.q),m=localMetrics(va,th,cal,cfg),score=specialistObjective(m);if(!best||score>best.score)best={cfg,th,validation:m,score}}}if(!best){out[rg]={off:true,activation:0,cfg:null,th:null,validation:null,score:-1e9};continue}const edge=best.validation.netGrowth,retEdge=best.validation.avgNetRet;let activation=clamp(.35+Math.max(0,edge)*5+Math.max(0,retEdge)*12,.25,1);const off=edge<-.015||retEdge<-.004||best.validation.maxDrawdown<-.12;out[rg]={...best,off,activation:off?0:activation}}return out}
function metaConfigs(){const out=[];for(const maxOpen of [5,8,12])for(const maxExposure of [.35,.55,.75])for(const dailyRiskBudget of [.02,.03,.045])for(const ddBrake of [.06,.09,.12])for(const oppFloor of [.25,.35,.45])out.push({maxOpen,maxExposure,dailyRiskBudget,ddBrake,ddScale:.5,oppFloor});return out}
function blendedCandidate(s,policies,cal,cfg){let weightedSize=0,weightedQuality=0,totalW=0,active=0;for(const [rg,p] of Object.entries(policies)){if(!p||p.off||!p.th)continue;const rw=s.regimeWeights[rg]||0;if(rw<.10)continue;const st=state(s,p.th);if(st.passed<p.cfg.consensus)continue;const ms=microScore(s.f,cal),baseQ=clamp((st.passed-p.cfg.consensus+1)/Math.max(1,6-p.cfg.consensus)),marginQ=clamp((st.margin+.15)/.75),quality=clamp((1-p.cfg.microWeight)*(.65*baseQ+.35*marginQ)+p.cfg.microWeight*ms);if(quality<p.cfg.minQuality)continue;const size=p.cfg.minSize+(p.cfg.maxSize-p.cfg.minSize)*Math.pow(quality,p.cfg.gamma),w=rw*p.activation;weightedSize+=w*size;weightedQuality+=w*quality;totalW+=w;active++}if(!totalW)return null;const opp=s.opportunity;let size=(weightedSize/totalW)*(0.65+0.70*opp)*clamp(totalW,.35,1.1);if(opp<cfg.oppFloor)size*=.55;return {...s,size,quality:weightedQuality/totalW,blendWeight:totalW,activeExperts:active}}
function metaMetrics(samples,policies,cal,cfg){const candidates=[];for(const s of samples){const c=blendedCandidate(s,policies,cal,cfg);if(c)candidates.push(c)}const sig=dedupe(candidates),allClean=new Set(samples.filter(s=>s.o.clean).map(s=>`${s.symbol}:${Math.floor(s.t/COOLDOWN)}`));let eq=1,peak=1,dd=0,open=[],admitted=[],skippedOpen=0,skippedExposure=0,skippedDaily=0;const dayRisk=new Map(),det=new Set();const closeUntil=t=>{const keep=[];for(const p of open){if(p.exitT<=t){eq*=1+p.size*p.o.net;peak=Math.max(peak,eq);dd=Math.min(dd,eq/peak-1)}else keep.push(p)}open=keep};for(const s of sig){closeUntil(s.t);if(open.length>=cfg.maxOpen){skippedOpen++;continue}let size=s.size;if(dd<=-cfg.ddBrake)size*=cfg.ddScale;const exposure=open.reduce((a,p)=>a+p.size,0);if(exposure+size>cfg.maxExposure)size=Math.max(0,cfg.maxExposure-exposure);if(size<.008){skippedExposure++;continue}const day=new Date(s.t).toISOString().slice(0,10),risk=size*HARD_STOP,used=dayRisk.get(day)||0;if(used+risk>cfg.dailyRiskBudget){skippedDaily++;continue}dayRisk.set(day,used+risk);admitted.push({...s,size});if(s.o.clean)det.add(`${s.symbol}:${Math.floor(s.t/COOLDOWN)}`);open.push({...s,size,exitT:s.t+s.o.holdingMin*60000})}closeUntil(Infinity);const clean=admitted.filter(s=>s.o.clean);return {signals:sig.length,admitted:admitted.length,clean:clean.length,winRate:admitted.length?admitted.filter(s=>s.o.net>0).length/admitted.length:0,precision:admitted.length?clean.length/admitted.length:0,recall:allClean.size?det.size/allClean.size:0,avgNetRet:avg(admitted.map(s=>s.o.net)),netGrowth:eq-1,maxDrawdown:dd,avgDetection24hPct:avg(clean.map(s=>s.f.r24*100)),stopBefore10Rate:admitted.length?admitted.filter(s=>s.o.stopBefore10).length/admitted.length:0,avgSize:avg(admitted.map(s=>s.size)),avgOpportunity:avg(admitted.map(s=>s.opportunity)),skippedOpen,skippedExposure,skippedDaily}}
function metaObjective(m){if(m.admitted<12)return-1e9;const ddPenalty=Math.max(0,Math.abs(m.maxDrawdown)-.12)*20,partPenalty=Math.max(0,.08-m.recall)*5;return m.netGrowth*7+m.avgNetRet*20+m.winRate*.7+m.precision*.9+m.recall*1.4-m.stopBefore10Rate*.55-ddPenalty-partPenalty}
function writeReport(r){const d=path.join('backend','training-output');fs.mkdirSync(d,{recursive:true});fs.writeFileSync(path.join(d,'spot-adaptive-mixture-v7_0-report.json'),JSON.stringify(r,null,2));console.log(JSON.stringify(r,null,2))}
async function main(){const syms=await universe();if(!syms.includes('BTCUSDT'))syms.unshift('BTCUSDT');console.log(`V7.0 lookback=${DAYS}d symbols=${syms.length}`);const data=new Map();for(const s of syms){try{const r=await klines(s);data.set(s,r);console.log(`LOADED ${s} ${r.length}`)}catch(e){console.log(`SKIP ${s} ${e.message}`)}}const bm=btcMap(data.get('BTCUSDT')||[]),raw=[],breadth=new Map();for(const [symbol,r] of data){if(symbol==='BTCUSDT'||r.length<WARM+FWD+2)continue;for(let i=WARM;i<r.length-FWD-1;i++){const f=feat(r,i,bm),t=r[i].t,b=breadth.get(t)||{n:0,up15:0,up60:0,breakout:0,ignite:0,sum60:0};b.n++;if(f.r15>0)b.up15++;if(f.r60>0)b.up60++;if(f.breakout60>0)b.breakout++;if(f.vol15>1.2)b.ignite++;b.sum60+=f.r60;breadth.set(t,b);if(f.qv<MIN_QV||f.r24<.001||f.r24>=.18||f.r60>=.10||f.r15>=.06)continue;const o=outcome(r,i);if(o)raw.push({symbol,t,f,o})}}for(const s of raw){s.breadth=breadthFrom(breadth,s.t);s.regimeWeights=regimeWeights(s.f,s.breadth);s.primaryRegime=Object.entries(s.regimeWeights).sort((a,b)=>b[1]-a[1])[0][0];s.opportunity=opportunityScore(s.breadth)}raw.sort((a,b)=>a.t-b.t);if(raw.length<500)return writeReport({generatedAt:new Date().toISOString(),version:'V7.0',researchOnly:true,productionTradingTouched:false,lookbackDays:DAYS,decision:{ready:false,label:'REQUIRES_MORE_DATA',reason:`Only ${raw.length} usable samples`}});const t0=raw[0].t,t1=raw.at(-1).t,span=t1-t0,c1=t0+span*.60,c2=t0+span*.80,train=raw.filter(x=>x.t<c1-PURGE),val=raw.filter(x=>x.t>c1+PURGE&&x.t<c2-PURGE),test=raw.filter(x=>x.t>c2+PURGE),cal=microCal(train),policies=chooseSpecialists(train,val,cal);let best=null;for(const cfg of metaConfigs()){const m=metaMetrics(val,policies,cal,cfg),score=metaObjective(m);if(!best||score>best.score)best={cfg,validation:m,score}}const testM=metaMetrics(test,policies,cal,best.cfg);const baseTh=thresholds(train,.92),baseCfg={consensus:4,microWeight:0,minQuality:0,minSize:.15,maxSize:.15,gamma:1},basePolicy={};for(const rg of ['TREND_UP','RANGE','VOLATILE','RISK_OFF'])basePolicy[rg]={off:false,activation:1,cfg:baseCfg,th:baseTh};const baseM=metaMetrics(test,basePolicy,cal,{maxOpen:99,maxExposure:9,dailyRiskBudget:9,ddBrake:9,ddScale:1,oppFloor:0});const ready=testM.netGrowth>0&&testM.avgNetRet>0&&testM.maxDrawdown>=-.12&&testM.netGrowth>baseM.netGrowth&&testM.avgDetection24hPct<=baseM.avgDetection24hPct+.25&&testM.recall>=Math.max(.08,baseM.recall*.65);writeReport({generatedAt:new Date().toISOString(),version:'V7.0',researchOnly:true,productionTradingTouched:false,lookbackDays:DAYS,objective:'soft regime mixture + cross-sectional breadth + confidence-weighted participation while preserving portfolio risk control',samples:{all:raw.length,train:train.length,validation:val.length,test:test.length},specialists:Object.fromEntries(Object.entries(policies).map(([r,p])=>[r,{off:p.off,activation:p.activation,cfg:p.cfg,validation:p.validation,score:p.score}])),metaController:{cfg:best.cfg,validation:best.validation,test:testM},baseline:{test:baseM},deltaVsBaseline:{netGrowth:testM.netGrowth-baseM.netGrowth,avgNetRet:testM.avgNetRet-baseM.avgNetRet,maxDrawdown:testM.maxDrawdown-baseM.maxDrawdown,recall:testM.recall-baseM.recall,precision:testM.precision-baseM.precision,detection:testM.avgDetection24hPct-baseM.avgDetection24hPct},decision:{ready,label:ready?'READY_FOR_SHADOW_VALIDATION':'REQUIRES_IMPROVEMENT',reason:ready?'Adaptive mixture is positive, drawdown-safe, participation-safe and economically superior on untouched test':'Adaptive mixture completed but did not satisfy all economic, risk, timing and participation requirements on untouched test'},promotion:ready?'SHADOW VALIDATION NEXT. No automatic production promotion.':'RESEARCH CONTINUES. Production unchanged.'})}
main().catch(e=>{console.error(e);process.exit(1)});
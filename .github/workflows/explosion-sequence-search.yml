name: Explosion Sequence Search
on:
  pull_request:
    paths:
      - 'backend/scripts/github-explosion-sequence-search.js'
      - '.github/workflows/explosion-sequence-search.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env: {REPLAY_DAYS: '35'}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: node --check backend/scripts/github-explosion-sequence-search.js
      - run: node backend/scripts/github-explosion-sequence-search.js
      - uses: actions/upload-artifact@v4
        with: {name: explosion-sequence-search, path: explosion-sequence-search.json, retention-days: 30}

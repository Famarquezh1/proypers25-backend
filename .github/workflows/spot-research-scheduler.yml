name: Spot research scheduler — GCP disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GCP research scheduler disabled. GitHub-native radar remains active."

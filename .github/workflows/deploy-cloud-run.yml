name: Deploy backend to Cloud Run — GCP disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Cloud Build/Cloud Run deployment disabled. GitHub-native Spot flow is active."

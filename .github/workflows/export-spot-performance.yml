name: Export Spot realized performance — GCP disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GCP/Firestore Spot performance export disabled."
